# Local Claude Code / editor config (machine-specific, not part of the product)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~
.vscode/
.idea/

# Blender auto-backup of the previous save
*.blend1

# Scratch render tests from building the Blender scenes (single frames used to
# eyeball a scene mid-build). The finished renders are media/diagram-*.mp4/png.
media/_test_*
